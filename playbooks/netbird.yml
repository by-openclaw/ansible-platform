---
# Configure PROD NetBird CE (lxc-netbird-01, SVC) — self-hosted Zero-Trust VPN
# (management + signal + relay + dashboard + coturn) with Authentik OIDC SSO.
# Replaces defguard. Requires infra-terraform-proxmox#46 (the LXC) applied first.
- name: Configure PROD NetBird CE
  hosts: netbird
  become: true
  gather_facts: true
  roles:
    - netbird
