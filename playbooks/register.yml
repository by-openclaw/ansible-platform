---
# ==============================================================================
# Render the platform service register (docs/register.md) from data — the audit's table of
# what exists, who owns it, how it is exposed, pinned, backed up, monitored, documented and
# how far its tuning pass is. Controller only; changes nothing on any host.
#   ansible-playbook -i inventories/prod/hosts.yml playbooks/register.yml
# ==============================================================================
- name: "Platform service register"
  hosts: localhost
  gather_facts: false
  become: false
  roles:
    - register
