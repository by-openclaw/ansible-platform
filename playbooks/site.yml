---
# ==============================================================================
# Playbook: site.yml — full platform playbook
# Imports all other playbooks in order.
# Run this for a full platform setup or use individual playbooks for targeted runs.
# ==============================================================================

- name: Import hardening playbook
  ansible.builtin.import_playbook: hardening.yml
