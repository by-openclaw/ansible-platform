---
# ==============================================================================
# Verdaccio — npm registry + upstream proxy (lxc-verdaccio-01, VPN-only).
# Guest = infra-terraform-proxmox svc-verdaccio.tf (VMID 505). Born through
# roles/service_scaffold (mailbox, Vault, Traefik route, logrotate).
#   ansible-playbook -i inventories/prod playbooks/verdaccio.yml
# ==============================================================================
- name: "Configure Verdaccio (npm registry)"
  hosts: verdaccio
  become: true
  gather_facts: true
  roles:
    - verdaccio
