---
# Configure PROD Nextcloud (lxc-nextcloud-01, SVC) — Docker files/drawio,
# Contabo S3 primary storage.
- name: Configure PROD Nextcloud
  hosts: nextcloud
  become: true
  gather_facts: true
  roles:
    - nextcloud
