---
# Configure PROD Vaultwarden (lxc-vaultwarden-01, SVC) — Docker password manager.
- name: Configure PROD Vaultwarden
  hosts: vaultwarden
  become: true
  gather_facts: true
  roles:
    - vaultwarden
