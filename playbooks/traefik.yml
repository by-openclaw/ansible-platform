---
# Configure PROD Traefik ingress (lxc-traefik-01, DMZ).
# Run: ansible-playbook -i inventories/prod/hosts.yml playbooks/traefik.yml
- name: Configure PROD Traefik reverse-proxy
  hosts: traefik
  become: true
  gather_facts: true
  roles:
    - traefik
