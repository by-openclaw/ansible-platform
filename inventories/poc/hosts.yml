---
all:
  children:

    proxmox_nodes:
      hosts:
        srv-proxmox-poc-01:
          ansible_host: 10.6.224.105
          ansible_port: 22222
          ansible_user: by-systems
          ansible_become: true

    opnsense:
      hosts:
        vm-opnsense-01:
          ansible_host: 10.1.1.1
          ansible_connection: local   # API-only, no SSH
          ansible_port: ~

    vms:
      hosts:
        vm-debian-bootstrap-test-01:
          ansible_host: 10.6.225.11
          ansible_port: 22222
          ansible_user: by-systems
          ansible_become: true
