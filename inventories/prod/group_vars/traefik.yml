---
# Traefik edge logs → Loki (infra/0006: every log stream shipped).
promtail_extra_file_jobs:
  - { name: traefik_access, path: /var/log/traefik/access.log }
  - { name: traefik_error, path: /var/log/traefik/traefik.log }
