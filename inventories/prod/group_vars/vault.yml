---
# Vault group specifics (NON-secret — secrets never live in inventory).
# SEC-07: promtail ships Vault's file audit device (bind-mount by roles/vault).
promtail_vault_audit_log: "/var/log/vault/audit.log"
