---
# ==============================================================================
# group_vars for the JumpServer bastion (lxc-jumpserver-01).
# ==============================================================================
# SSO is mandatory platform-wide: log in through Authentik (OIDC). Client creds
# live in Vault (fabric/app-oidc-jumpserver), minted by the authentik role.
jumpserver_oidc_enabled: true
