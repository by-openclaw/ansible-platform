---
# Admin break-glass desktop allowed into internal-only Traefik routes from the
# OOB plane (BY-DESK-03). Interim access path until the NetBird VPN is the path —
# see reference_offnet_desktop_access_model. Remove once VPN is the access path.
traefik_route_admin_cidrs:
  - "10.6.239.113/32"
