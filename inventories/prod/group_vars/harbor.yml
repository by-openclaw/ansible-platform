---
# Harbor components log via the syslog driver into /var/log/harbor/*.log
# (harbor's own design) — ship them to Loki; rotation = roles/logrotate.
promtail_extra_file_jobs:
  - { name: harbor_components, path: "/var/log/harbor/*.log" }
