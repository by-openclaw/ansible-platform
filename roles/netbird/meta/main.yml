---
galaxy_info:
  role_name: netbird
  author: BY-SYSTEMS SRL
  description: NetBird CE (Docker) — management+signal+relay+dashboard+coturn, shared Postgres store, Authentik OIDC SSO, behind Traefik (h2c).
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
