---
galaxy_info:
  role_name: netbird
  author: BY-SYSTEMS SRL
  description: >-
    NetBird CE (official combined model) — netbird-server (mgmt+signal+relay+STUN
    +embedded Dex) + dashboard, sqlite store, Authentik external OIDC IdP, behind
    Traefik (h2c for gRPC). Ansible-only reproduction of getting-started.sh.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
