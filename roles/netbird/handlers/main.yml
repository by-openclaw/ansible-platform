---
- name: "Restart netbird management"
  community.docker.docker_container:
    name: netbird-management
    state: started
    restart: true
  listen: "Restart netbird management"

- name: "Restart netbird dashboard"
  community.docker.docker_container:
    name: netbird-dashboard
    state: started
    restart: true
  listen: "Restart netbird dashboard"

- name: "Restart netbird coturn"
  community.docker.docker_container:
    name: netbird-coturn
    state: started
    restart: true
  listen: "Restart netbird coturn"
  when: netbird_coturn_enabled | bool
