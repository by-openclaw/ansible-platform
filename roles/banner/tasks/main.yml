---
- name: "Banner | Deploy /etc/issue + /etc/issue.net (Lynis BANN-7126/7130)"
  ansible.builtin.copy:
    dest: "{{ item }}"
    content: "{{ banner_text }}"
    owner: root
    group: root
    mode: "0644"
  loop: [/etc/issue, /etc/issue.net]
