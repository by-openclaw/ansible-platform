{# Cloudflare DNS-01 API credentials for lego — rendered 0600, root-only. #}
{# Sourced by /etc/lego/renew-traefik.sh. Same scoped token for both vars. #}
CF_DNS_API_TOKEN={{ traefik_cf_token }}
CF_ZONE_API_TOKEN={{ traefik_cf_token }}
