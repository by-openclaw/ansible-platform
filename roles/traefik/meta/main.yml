---
galaxy_info:
  role_name: traefik
  author: BY-SYSTEMS SRL
  description: >-
    Traefik v3 reverse-proxy / HTTPS ingress on a dedicated LXC. ACME DNS-01
    (Cloudflare) wildcard certs via lego; file-provider dynamic config.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base   # baseline packages (curl, dnsutils, …) + sshd/cron first
