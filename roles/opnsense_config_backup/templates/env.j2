# Managed by Ansible (roles/opnsense_config_backup) — root 0600
FW_URL="{{ opnsense_config_backup_fw_url }}"
FW_RESOLVE="{{ opnsense_config_backup_fw_resolve }}"
FW_KEY="{{ opnsense_config_backup_fw_key }}"
FW_SECRET="{{ opnsense_config_backup_fw_secret }}"
AGE_RECIPIENT="{{ opnsense_config_backup_age_public }}"
