# Managed by Ansible (role: wazuh). Dashboard → manager API (service user from Vault).
hosts:
  - default:
      url: "https://wazuh.manager"
      port: 55000
      username: {{ wazuh_api_user }}
      password: "{{ wazuh_api_password }}"
      run_as: true
