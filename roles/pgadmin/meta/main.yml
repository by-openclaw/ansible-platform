---
galaxy_info:
  role_name: pgadmin
  author: BY-SYSTEMS SRL
  description: pgAdmin 4 (Docker) DB admin UI, internal-only behind Traefik.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
