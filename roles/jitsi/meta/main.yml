---
galaxy_info:
  role_name: jitsi
  author: BY-SYSTEMS
  description: Jitsi Meet video conferencing (official docker-jitsi-meet) — Docker-in-LXC, Traefik TLS, JVB UDP media, secrets in Vault, Authentik SSO.
  license: MIT
  min_ansible_version: "2.15"
dependencies:
  - role: docker
