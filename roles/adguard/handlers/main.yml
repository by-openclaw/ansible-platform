---
# ==============================================================================
# Role: adguard — handlers (triggered by notify)
# ==============================================================================

- name: Restart AdGuardHome
  ansible.builtin.systemd:
    name: AdGuardHome
    state: restarted
    enabled: true
