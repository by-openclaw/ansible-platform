{# Cloudflare DNS-01 API credentials for lego — rendered 0600, root-only #}
{# Both vars use the same scoped token (mirrors setup-adguard-tls.sh). #}
CF_DNS_API_TOKEN={{ adguard_cf_token }}
CF_ZONE_API_TOKEN={{ adguard_cf_token }}
