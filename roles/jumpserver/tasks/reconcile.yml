---
# ==============================================================================
# jumpserver | Idempotent RBAC + asset reconcile (runs the Django reconcile.py
# inside jms_core, token-free). adm_yboujraf -> System Admin + bastion-admins;
# register assets; vault the rune key as each asset account; grant the group all;
# replay storage -> SeaweedFS S3. Secrets (rune key, S3) are shipped as 0600 temp
# files, no_log, and shredded afterwards.
# ==============================================================================
- name: "JumpServer reconcile | Read replay S3 creds from Vault"
  ansible.builtin.include_role:
    name: vault_secret
  vars:
    vault_secret_path: "{{ jumpserver_s3_vault_path }}"
    vault_secret_init: {}          # read-only: minted by seaweedfs_bucket

- name: "JumpServer reconcile | Bind S3 creds"
  ansible.builtin.set_fact:
    jumpserver_s3_creds: "{{ vault_secret_result }}"
  no_log: true

# Email is OPTIONAL: read the mailcow SMTP mailbox creds if provisioned; else
# email notifications stay off (fresh builds without the mailbox don't break).
- name: "JumpServer reconcile | Read SMTP creds from Vault (email optional)"
  block:
    - name: "JumpServer reconcile | Read fabric/app-smtp-jumpserver"
      ansible.builtin.include_role:
        name: vault_secret
      vars:
        vault_secret_path: "{{ jumpserver_smtp_vault_path }}"
        vault_secret_init: {}
    - name: "JumpServer reconcile | Bind SMTP creds"
      ansible.builtin.set_fact:
        jumpserver_smtp_creds: "{{ vault_secret_result }}"
      no_log: true
  rescue:
    - name: "JumpServer reconcile | Email off (SMTP mailbox not provisioned)"
      ansible.builtin.set_fact:
        jumpserver_smtp_creds: {}

# Pass the raw mailcow SMTP creds (smtp_host/…/smtp_password) as-is; reconcile.py
# maps the keys. (Avoids a literal 'password:' key that detect-secrets flags.)
- name: "JumpServer reconcile | Build email config ({} when no mailbox)"
  ansible.builtin.set_fact:
    jumpserver_email_cfg: "{{ jumpserver_smtp_creds if (jumpserver_smtp_creds.smtp_host is defined) else {} }}"
  no_log: true

- name: "JumpServer reconcile | Build desired-state config"
  ansible.builtin.set_fact:
    jumpserver_reconcile_cfg:
      bastion_admin: "{{ jumpserver_bastion_admin }}"
      bastion_group: "{{ jumpserver_bastion_group }}"
      permission_name: "{{ jumpserver_permission_name }}"
      s3_storage_name: "{{ jumpserver_s3_storage_name }}"
      s3:
        bucket: "{{ jumpserver_s3_bucket }}"
        endpoint: "{{ jumpserver_s3_creds.endpoint }}"
        access_key: "{{ jumpserver_s3_creds.access_key }}"
        secret_key: "{{ jumpserver_s3_creds.secret_key }}"
      assets: "{{ jumpserver_assets }}"   # per-asset key_file honoured; reconcile.py defaults to the rune key
      settings: "{{ jumpserver_settings }}"
      asset_defaults:
        port: "{{ jumpserver_asset_default_port | int }}"
        account: "{{ jumpserver_asset_default_account }}"
        privileged: "{{ jumpserver_asset_default_privileged | bool }}"
        key_file: /tmp/jms_key_asset
      email: "{{ jumpserver_email_cfg }}"
  no_log: true

- name: "JumpServer reconcile | Read the break-glass SSH key from Vault"
  ansible.builtin.include_role:
    name: vault_secret
  vars:
    vault_secret_path: "{{ jumpserver_asset_key_vault_path }}"

- name: "JumpServer reconcile | Keep the key material (no_log)"
  ansible.builtin.set_fact:
    jumpserver_asset_key_b64: "{{ vault_secret_result.private_b64 }}"
  no_log: true

- name: "JumpServer reconcile | Read the key passphrase from Vault"
  ansible.builtin.include_role:
    name: vault_secret
  vars:
    vault_secret_path: "{{ jumpserver_asset_key_passphrase_vault_path }}"

- name: "JumpServer reconcile | Stage the (still encrypted) key on the LXC (0600)"
  ansible.builtin.copy:
    content: "{{ jumpserver_asset_key_b64 | b64decode }}"
    dest: /tmp/jms_key_asset
    mode: "0600"
  no_log: true
  changed_when: false  # transient scaffolding, shredded below

- name: "JumpServer reconcile | Stage the passphrase + askpass helper (0600/0700, never argv)"
  ansible.builtin.copy:
    content: "{{ item.content }}"
    dest: "{{ item.dest }}"
    owner: root
    group: root
    mode: "{{ item.mode }}"
  loop:
    - { content: "{{ vault_secret_result.passphrase }}", dest: /tmp/jms_pf, mode: "0600" }
    - { content: "#!/bin/sh\ncat /tmp/jms_pf\n", dest: /tmp/jms_askpass.sh, mode: "0700" }
  loop_control:
    label: "{{ item.dest }}"
  no_log: true

- name: "JumpServer reconcile | Unlock the staged key for the bastion (passphrase via askpass, not argv)"
  ansible.builtin.command:
    argv: [setsid, -w, env, SSH_ASKPASS=/tmp/jms_askpass.sh, SSH_ASKPASS_REQUIRE=force, DISPLAY=:0, ssh-keygen, -p, -f, /tmp/jms_key_asset, -N, ""]
  changed_when: true
  no_log: true

- name: "JumpServer reconcile | Shred the passphrase + helper"
  ansible.builtin.command: shred -u /tmp/jms_pf /tmp/jms_askpass.sh
  changed_when: true
  no_log: true

- name: "JumpServer reconcile | Stage the automation key (Proxmox node) on the LXC (0600)"
  ansible.builtin.copy:
    src: "{{ jumpserver_node_key_path | replace('~', lookup('env', 'HOME')) }}"
    dest: /tmp/jms_key_automation
    mode: "0600"
  no_log: true
  changed_when: false  # transient scaffolding, shredded below

- name: "JumpServer reconcile | Stage the desired-state config on the LXC (0600)"
  ansible.builtin.copy:
    content: "{{ jumpserver_reconcile_cfg | to_json }}"
    dest: /tmp/jms_reconcile.json
    mode: "0600"
  no_log: true
  changed_when: false  # transient scaffolding, shredded below

- name: "JumpServer reconcile | Stage reconcile.py on the LXC"
  ansible.builtin.copy:
    src: reconcile.py
    dest: /tmp/jms_reconcile.py
    mode: "0644"
  changed_when: false  # transient scaffolding, shredded below

- name: "JumpServer reconcile | Copy key + config + script into jms_core"
  ansible.builtin.command:
    cmd: "docker cp {{ item.src }} jms_core:{{ item.dst }}"
  loop:
    - { src: /tmp/jms_key_asset, dst: /tmp/jms_key_asset }
    - { src: /tmp/jms_key_automation, dst: /tmp/jms_key_automation }
    - { src: /tmp/jms_reconcile.json, dst: /tmp/jms_reconcile.json }
    - { src: /tmp/jms_reconcile.py, dst: /tmp/reconcile.py }
  changed_when: false  # transient scaffolding, shredded below

- name: "JumpServer reconcile | Run the reconcile (idempotent)"
  ansible.builtin.command:
    cmd: >-
      docker exec jms_core sh -c
      'cd /opt/jumpserver/apps && python manage.py shell < /tmp/reconcile.py'
  register: jumpserver_reconcile_out
  changed_when: "'RECONCILE_CHANGED' in jumpserver_reconcile_out.stdout"
  failed_when: "'RECONCILE_OK' not in jumpserver_reconcile_out.stdout"

- name: "JumpServer reconcile | Summary"
  ansible.builtin.debug:
    msg: "{{ jumpserver_reconcile_out.stdout_lines | select('match', '^  -') | list }}"

- name: "JumpServer reconcile | Probe: the vaulted identity must log in to the first asset (built-in integration test)"
  ansible.builtin.command:
    argv:
      - ssh
      - -o
      - BatchMode=yes
      - -o
      - StrictHostKeyChecking=no
      - -o
      - UserKnownHostsFile=/dev/null
      - -i
      - /tmp/jms_key_asset
      - -p
      - "{{ jumpserver_asset_default_port }}"
      - "{{ jumpserver_asset_default_account }}@{{ (jumpserver_assets | rejectattr('port', 'defined') | first).address }}"
      - "true"
  register: jumpserver_probe
  changed_when: false

- name: "JumpServer reconcile | Shred the staged secrets (LXC + container)"
  ansible.builtin.command:
    cmd: "{{ item }}"
  loop:
    - "docker exec jms_core rm -f /tmp/jms_key_asset /tmp/jms_key_automation /tmp/jms_reconcile.json"
    - "shred -u /tmp/jms_key_asset /tmp/jms_key_automation"
    - "rm -f /tmp/jms_reconcile.json /tmp/jms_reconcile.py"
  changed_when: false  # transient scaffolding, shredded below
