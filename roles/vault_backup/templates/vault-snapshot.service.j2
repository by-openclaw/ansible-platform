[Unit]
# Managed by Ansible (role vault_backup).
Description=Vault raft snapshot (DR backup)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/vault-snapshot.sh
