---
- name: Restart alertmanager
  ansible.builtin.systemd_service:
    name: prometheus-alertmanager
    state: restarted
  when: not ansible_check_mode   # the unit does not exist yet on a fresh host under --check
