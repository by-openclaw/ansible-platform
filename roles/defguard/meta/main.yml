---
galaxy_info:
  role_name: defguard
  author: BY-SYSTEMS SRL
  description: Defguard WireGuard VPN (Docker) — core+proxy+userspace gateway, Authentik OIDC SSO, shared Postgres, behind Traefik.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
