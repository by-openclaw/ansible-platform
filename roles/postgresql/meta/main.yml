---
galaxy_info:
  role_name: postgresql
  author: BY-SYSTEMS SRL
  description: PostgreSQL 17 shared cluster DB — data-persistent, HA-adoptable.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
