---
galaxy_info:
  role_name: docker
  author: BY-SYSTEMS SRL
  description: Docker engine + python SDK for nested service LXCs (Debian distro packages).
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
