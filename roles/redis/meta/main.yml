---
galaxy_info:
  role_name: redis
  author: BY-SYSTEMS SRL
  description: Shared Redis (Docker) — TLS (wildcard cert), auth, AOF-durable, HA-adoptable.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
