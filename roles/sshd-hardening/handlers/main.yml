---
# The config was already syntax-validated by the template task's
# `validate: /usr/sbin/sshd -t -f %s` — the handler only restarts.
- name: Validate and restart sshd
  ansible.builtin.service:
    name: sshd
    state: restarted
  when: not ansible_check_mode
