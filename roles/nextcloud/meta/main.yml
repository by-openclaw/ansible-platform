---
galaxy_info:
  role_name: nextcloud
  author: BY-SYSTEMS SRL
  description: Nextcloud (Docker) — web+cron, Contabo S3 primary storage, shared Postgres+Redis, drawio, behind Traefik.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
