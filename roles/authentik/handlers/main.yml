---
# Restart Authentik server + worker (the worker applies blueprints on start).
- name: Restart authentik
  community.docker.docker_container:
    name: "{{ item }}"
    state: started
    restart: true
  loop:
    - authentik-server
    - authentik-worker
