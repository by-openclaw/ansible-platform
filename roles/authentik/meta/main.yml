---
galaxy_info:
  role_name: authentik
  author: BY-SYSTEMS SRL
  description: Authentik SSO/IdP (Docker) — server+worker, shared Postgres+Redis, behind Traefik.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
