---
galaxy_info:
  role_name: gitlab_runner
  author: BY-SYSTEMS
  description: >-
    GitLab CI runner (Docker executor + Kaniko-based image builds) on an isolated
    VM. Mints a GitLab 17+ instance-runner auth token on the server, installs
    Docker + gitlab-runner, and renders config.toml declaratively.
  license: proprietary
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
dependencies: []
