---
galaxy_info:
  role_name: vaultwarden
  author: BY-SYSTEMS SRL
  description: Vaultwarden (Bitwarden-compatible password manager, Docker) — shared Postgres, behind Traefik.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
