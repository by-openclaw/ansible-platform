# Managed by ansible (minio role) — do not edit by hand.
MINIO_ROOT_USER={{ minio_creds.root_user }}
MINIO_ROOT_PASSWORD={{ minio_creds.root_password }}
MINIO_VOLUMES={{ minio_data_dir }}
MINIO_OPTS=--address {{ minio_listen_ip }}:{{ minio_api_port }} --console-address {{ minio_listen_ip }}:{{ minio_console_port }}
MINIO_REGION={{ minio_region }}
MINIO_SERVER_URL=https://{{ minio_fqdn }}
