# Managed by Ansible (role: k3s). Registry mirrors for containerd.
mirrors:
{% for m in k3s_registry_mirrors %}
  "{{ m.upstream }}":
    endpoint:
      - "{{ m.endpoint }}"
    rewrite:
      "{{ m.rewrite_from }}": "{{ m.rewrite_to }}"
{% endfor %}
