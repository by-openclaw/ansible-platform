---
galaxy_info:
  role_name: netbox
  author: BY-SYSTEMS SRL
  description: NetBox IPAM/DCIM (Docker) — web+worker+housekeeping, shared Postgres+Redis, behind Traefik.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
