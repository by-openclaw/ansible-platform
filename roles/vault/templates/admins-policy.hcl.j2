{# Managed by Ansible role `vault`. Vault admins policy (mapped from the Authentik
   `vault-admins` group via OIDC). Full management incl. sudo. #}
path "*" {
  capabilities = ["create", "read", "update", "delete", "list", "sudo"]
}
