---
galaxy_info:
  role_name: vault
  author: BY-SYSTEMS SRL
  description: HashiCorp Vault (Docker) — raft storage, TLS wildcard, behind Traefik.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: base
  - role: docker
