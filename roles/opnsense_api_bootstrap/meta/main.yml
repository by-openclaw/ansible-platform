---
galaxy_info:
  role_name: opnsense_api_bootstrap
  author: by-rune
  description: >-
    Mint the svc-ansible OPNsense API identity through the MVC API
    (auth/user/add_api_key) and store the token in Vault. Get-or-mint,
    idempotent. Genesis = a break-glass admin key (the one seeded credential).
  license: MIT
  min_ansible_version: "2.15"
dependencies: []
