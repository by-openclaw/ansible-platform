---
# gitlab-ctl reconfigure IS the config compiler/validator: a malformed gitlab.rb
# or an unreachable dependency (PG/Redis/S3) surfaces here with a non-zero rc.
- name: Reconfigure GitLab
  ansible.builtin.command: gitlab-ctl reconfigure
  changed_when: true
