---
galaxy_info:
  role_name: gitlab
  author: BY-SYSTEMS
  description: >-
    GitLab CE (official Omnibus) wired to the shared cluster services — external
    PostgreSQL / Redis (TLS) / SeaweedFS S3, Authentik OIDC SSO, mailcow SMTP,
    container registry, Pages, and S3 backups. Git repos on a persistent ZFS
    bind-mount; nothing bundled (PG/Redis/object-store all disabled).
  license: proprietary
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies: []
