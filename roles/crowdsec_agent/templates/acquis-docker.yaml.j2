# Managed by ansible (crowdsec_agent role) — container log acquisition.
# CrowdSec reads these containers' stdout via the Docker socket; the services
# themselves are not modified and write no host log files.
source: docker
container_name:
{% for c in item.containers %}
  - {{ c }}
{% endfor %}
labels:
  type: {{ item.type }}
