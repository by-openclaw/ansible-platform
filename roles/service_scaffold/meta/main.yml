---
galaxy_info:
  role_name: service_scaffold
  author: BY-SYSTEMS
  description: >-
    Golden path for a new platform service — composes the platform concern-roles
    (Vault, mailbox, Postgres/S3, Traefik, logrotate) from one service contract
    and asserts the central SSO/firewall/audit registrations are present.
  license: MIT
  min_ansible_version: "2.15"
dependencies: []
