# role: service_scaffold

Golden path for a new service — composes Postgres/S3/Vault/mailbox/Traefik/logrotate from one `service_scaffold_def` and asserts the central SSO/firewall/audit registrations.

See `docs/new-service.md`.
