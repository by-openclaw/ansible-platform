---
# ansible-lint project config. Archived (superseded, never-deleted) task files and
# playbooks are kept for history only and are not part of the linted contract.
exclude_paths:
  - roles/opnsense/tasks/_archive/
  - playbooks/_archive/
  - .cache/
