---
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.postgresql
    # postgresql_db / postgresql_user / postgresql_pg_hba — cluster DB role.
    version: ">=3.0.0"
  - name: community.docker
    # docker_image / docker_container — pgadmin (and future Docker svc).
    version: ">=3.10.0"
  - name: ansibleguy.opnsense
    # API-based (alias/rule/wireguard) — no root needed.
    version: ">=1.2.0"
  - name: by_systems.opnsense
    # The MVC lib (lib-opnsense) — API-driven managers, no root.
    version: ">=0.2.0"
